-- Today, this session could be a bit short because I've something else to get to and I was also building python. 
-- Let's first try to find out the reasons for NULL's. 
SELECT 
    order_approved_at,
    order_status,
    count(*)
FROM
 olist_database.olist_orders
GROUP BY
 order_status, order_approved_at
HAVING
 order_approved_at IS NULL;
/* The result is a bit tricky, the total NULL's are 160, out of which 141 orders were places but later cancelled,
5 orders were placed but they didn't move forward from their, but 14 orders status is 'delivered' which is not possible,
Because if they're delivered, they should have the timestamp of when the order was approved. */

-- I'm leaving it here for today or if I finish my extra task for today early, I'll get back to this. 
